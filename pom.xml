<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <packaging>pom</packaging>
    <groupId>com.metanet4j</groupId>
    <artifactId>metanet4j-parent</artifactId>
    <version>${metanet4j.version}</version>
    <name>metanet4j-parent</name>
    <description>metanet4j parent</description>
    <properties>
        <metanet4j.version>0.2.0</metanet4j.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Java 25：Boot 4.1 支持 17–26；release 统一控制编译目标 -->
        <java.version>25</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>

        <spring-cloud.version>2025.1.3</spring-cloud.version>

        <hutool-all.version>5.7.12</hutool-all.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <!-- 保留：Boot BOM 不管理 guava，且 30.1.1-jre 在 JDK 25 下可编译（计划 §6.2） -->
        <guava.version>30.1.1-jre</guava.version>
        <junit.version>4.13.2</junit.version>
        <!-- 保留：fastjson 已停更，本次不动（计划 §6.2） -->
        <fastjson.version>1.2.76</fastjson.version>
        <!-- 保留：安全升级单独立项，本次不动（计划 §6.2） -->
        <bcprov-jdk18on.version>1.71</bcprov-jdk18on.version>

        <druid.version>1.2.28</druid.version>

        <jsonrpc4j>1.0</jsonrpc4j>
        <jeromq.version>0.5.2</jeromq.version>

        <bitcoinjsv.version>1.0.4</bitcoinjsv.version>
        <bitcoinsv-jcl.version>1.0.5</bitcoinsv-jcl.version>

        <!--sql -->
        <mybatis-plus>3.5.17</mybatis-plus>
        <mybatis-plus-generator>3.5.17</mybatis-plus-generator>
        <velocity-engine-core>2.4.1</velocity-engine-core>

        <!-- es -->
        <elasticsearch.client.version>9.4.5</elasticsearch.client.version>

        <!-- swagger（仅注解，不引 springdoc starter） -->
        <swagger-annotations-jakarta.version>2.2.55</swagger-annotations-jakarta.version>

        <!-- mapstruct -->
        <org.mapstruct.version>1.5.5.Final</org.mapstruct.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>

        <jsonwebtoken.version>0.13.0</jsonwebtoken.version>
        <!-- 注意：lombok 版本不再在此钉死（随 Boot BOM 1.18.46）；
             annotationProcessorPaths 里用字面量 1.18.46，见复评 P1-1 -->
    </properties>


    <dependencyManagement>
        <dependencies>

            <dependency>
                <groupId>com.metanet4j</groupId>
                <artifactId>metanet4j-base</artifactId>
                <version>0.2.0</version>
            </dependency>

            <dependency>
                <groupId>com.metanet4j</groupId>
                <artifactId>metanet4j-sdk</artifactId>
                <version>0.2.0</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-logging</artifactId>
                <exclusions>
                    <exclusion>
                        <groupId>org.apache.logging.log4j</groupId>
                        <artifactId>log4j-to-slf4j</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- log4j-slf4j2-impl 不在此声明：版本随 Boot BOM（log4j-bom 2.25.5）。
                 经验证，留一条不带 <version> 的 depMgmt 条目会屏蔽 BOM 版本管理，
                 子模块会直接报 version is missing（2026-09-16 实测） -->

            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool-all.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>


            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
                <scope>compile</scope>
            </dependency>


            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>fastjson</artifactId>
                <version>${fastjson.version}</version>
            </dependency>
            <!-- mysql-connector-j 不在此声明：版本随 Boot BOM（9.7.0）；
                 不带 <version> 的条目会屏蔽 BOM（见上） -->

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-4-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.briandilley.jsonrpc4j</groupId>
                <artifactId>jsonrpc4j</artifactId>
                <version>${jsonrpc4j}</version>
            </dependency>


            <dependency>
                <groupId>org.zeromq</groupId>
                <artifactId>jeromq</artifactId>
                <version>${jeromq.version}</version>
            </dependency>

            <!-- sql-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-generator</artifactId>
                <version>${mybatis-plus-generator}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity-engine-core}</version>
            </dependency>

            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus}</version>
            </dependency>

            <dependency>
                <groupId>io.bitcoinsv.bitcoinjsv</groupId>
                <artifactId>bitcoinj-base</artifactId>
                <version>${bitcoinjsv.version}</version>
            </dependency>


            <dependency>
                <groupId>io.bitcoinsv.jcl</groupId>
                <artifactId>jcl-store-levelDB</artifactId>
                <version>${bitcoinsv-jcl.version}</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>io.bitcoinsv.jcl</groupId>
                <artifactId>jcl-store</artifactId>
                <version>${bitcoinsv-jcl.version}</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>io.bitcoinsv.jcl</groupId>
                <artifactId>jcl-store-keyValue</artifactId>
                <version>${bitcoinsv-jcl.version}</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>io.bitcoinsv.jcl</groupId>
                <artifactId>jcl-tools</artifactId>
                <version>${bitcoinsv-jcl.version}</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>io.bitcoinsv.jcl</groupId>
                <artifactId>jcl-net</artifactId>
                <version>${bitcoinsv-jcl.version}</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bcprov-jdk18on.version}</version>
            </dependency>

            <dependency>
                <groupId>co.elastic.clients</groupId>
                <artifactId>elasticsearch-java</artifactId>
                <version>${elasticsearch.client.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- 代码只用注解；不引 springdoc starter（计划 §1 范围外） -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations-jakarta.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${org.mapstruct.version}</version>
            </dependency>

            <!-- JSON Web Token Support（0.9.1 依赖 javax.xml.bind，Java 25 下不可用） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jsonwebtoken.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jsonwebtoken.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jsonwebtoken.version}</version>
                <scope>runtime</scope>
            </dependency>
        </dependencies>

    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${org.mapstruct.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <!-- 版本随 spring-boot-starter-parent 的 pluginManagement（4.1.1 → 3.15.0） -->
                <configuration>
                    <!-- 编译目标由 <maven.compiler.release>${java.version}</maven.compiler.release> 控制 -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${org.mapstruct.version}</version>
                        </path>

                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <!-- 字面量：与 Boot 4.1.1 BOM 的 1.18.46 对齐（复评 P1-1：
                                 该属性已删除，不能再用 ${lombok.version}） -->
                            <version>1.18.46</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>
                        <!-- other annotation processors -->
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <!-- 版本由 spring-boot-starter-parent 的 pluginManagement 提供（3.6.3）
                     rules 放在插件级（而非 execution 内）：这样 `mvn enforcer:enforce`
                     命令行调用与 validate 阶段绑定执行两条路径都能生效 -->
                <configuration>
                    <rules>
                        <requireJavaVersion>
                            <version>[25,)</version>
                            <message>本项目要求 JDK 25+（Boot 4.1.1 / Java 25 升级）</message>
                        </requireJavaVersion>
                        <bannedDependencies>
                            <excludes>
                                <exclude>javax.*:*</exclude>
                            </excludes>
                            <!-- 白名单：Redisson 4.7.0 传递引入，短期无法移除；
                                 若 P4 在 component-cache 排除该依赖，可删掉此行 -->
                            <includes>
                                <include>javax.cache:cache-api</include>
                            </includes>
                            <message>javax.* 禁止进入依赖树（迁移到 jakarta / org.jspecify）</message>
                        </bannedDependencies>
                    </rules>
                </configuration>
                <executions>
                    <execution>
                        <id>enforce-java-25-and-banned-javax</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <attach>true</attach>
                </configuration>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>



</project>
